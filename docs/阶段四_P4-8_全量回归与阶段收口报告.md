# 阶段四 P4-8：全量回归与阶段收口报告

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 阶段 | 阶段四 P4-8——全量回归与阶段收口 |
| 日期 | 2026-08-03 |
| 分支 | `refactor/phase4-small-steps` |
| P4-8 起点 | `8187088` |
| 最终构建目录 | `build_stage4_p48_final` |
| 当前状态 | 自动化与 SAM GUI 验收通过；P4-8 已提交并推送（`2eed812`） |

## 2. 自动化结论

P4-8 已完成全新目录配置、Release 全构建、C++ 全量测试、Node 全量测试和三份几何基线重算。

> 最终全新构建成功；CTest 14/14 通过；Node 9 个测试文件及几何基线门禁通过；三份 DXF 的输入哈希、输出数量、包围盒和几何指纹与阶段一完全一致。自动化范围内没有发现阶段四回归。

SAM GUI 的对象树、Viewport、真实 Python/SAMSDK 调用、取消响应和峰值内存已经完成人工验收，阶段四最终结论为“通过”。

## 3. P4-0～P4-7 交付记录

| 批次 | 提交 | 交付主题 | 状态 |
|---|---|---|---|
| P4-0 | `c436c64` | 基线冻结与现状核查 | 已推送、用户通过 |
| P4-1 | `66f6bec` | 结构化失败、取消和回滚结果 | 已推送、用户通过 |
| P4-2 | `9f58245` | 请求预检、应用服务和 Python 入口收口 | 已推送、用户通过 |
| P4-3 | `0043ec6` | Parser 与 BLOCK/INSERT 结果边界 | 已推送、用户通过 |
| P4-4 | `a698b21` | 数值谓词、POINT 语义和 FE 空间索引 | 已推送、用户通过 |
| P4-5 | `85d5c43` | CMake 依赖目标和测试构建 | 已推送、用户通过 |
| P4-6 | `786b4f7` | Web 与 Server 模块边界 | 已推送、用户通过 |
| P4-7 | `8187088` | GUI、进度和日志生命周期 | 已推送、用户通过 |

各批次均可单独定位和回退，没有将阶段四积累成单个大提交。

## 4. 最终干净构建

### 4.1 配置命令

```powershell
cmake -S . -B build_stage4_p48_final -G "Visual Studio 15 2017" -A x64 `
  -DEXAMPLE1_BUILD_TESTS=ON `
  -DSPDLOG_ROOT="D:/vcpkg/installed/x64-windows" `
  -DFETCHCONTENT_SOURCE_DIR_GOOGLETEST="D:/shixiSoftware/Homework/7-dxf_to_sam/build/_deps/googletest-src"
```

结果：

```text
Configuring done
Generating done
Build files have been written to: .../build_stage4_p48_final
```

### 4.2 Release 构建

```powershell
cmake --build build_stage4_p48_final --config Release --parallel 4
```

结果：通过。正式产物：

| 文件 | 大小 |
|---|---:|
| `bin/Release/Example1.pyd` | 715,776 字节 |
| `bin/Release/SAM.Pre.Example1Toolset.dll` | 301,568 字节 |

产物名称、输出位置和部署方式未改变。

构建日志中的 C4828 和 C5033 仍来自 Qtitan 与 Python 2.7 第三方头文件，没有新增项目源码编译错误。

## 5. 最终测试结果

### 5.1 C++

```powershell
ctest --test-dir build_stage4_p48_final -C Release --output-on-failure
```

结果：

```text
100% tests passed, 0 tests failed out of 14
```

覆盖：

```text
GeometryUtils
DxfData
ConversionEngine
Convert
FeConversionEngine
Parser
DxfImportLogger
ImportTransaction
DxfImportValidation
DxfImportMode
DxfImportLayers
DxfBlockExpansion
DxfTransformedCurves
ImportBuildService
```

### 5.2 Web 与 Server

```powershell
npm.cmd run check
npm.cmd test
```

结果：

- 语法检查通过；
- 9 个 Node 测试文件全部通过；
- Server 路由、安全、Agent 边界和 Web 模块测试通过；
- 几何基线脚本作为 `npm test` 的最后一道门禁自动执行。

## 6. 可重复几何基线门禁

新增：

```text
scripts/verify-geometry-baseline.js
```

运行：

```powershell
npm.cmd run test:geometry-baseline
```

该脚本固定检查：

- DXF 输入文件 SHA-256；
- 输出点和线数量；
- `minX/minY/maxX/maxY`；
- 点、线、图层、来源类型和不支持类型的确定性 SHA-256。

任意坐标、顺序、图层、来源类型或不支持实体统计变化都会使门禁失败。

### 6.1 最终结果

| 文件 | 点/线 | 包围盒 `[minX,minY,maxX,maxY]` | 输出指纹 | 结论 |
|---|---:|---|---|---|
| `block_test_minimal.dxf` | 0 / 3 | `[400,300,1100,1000]` | `E9E68AA652D2A17D2365B559FEA2F6B2027784686C3689471E72D3B53EEDAB19` | 与阶段一一致 |
| `slock_spline_zhenlie.dxf` | 0 / 49,512 | `[8405.880227,4370.679497,86281.658819,35596.279992]` | `143835F9F0EB5EC2770626D8669C81CFBCD36D2EFAF76CA2F21FD5928F62B9AD` | 与阶段一一致 |
| `ship2_block_test.dxf` | 92 / 47,611 | `[178224.717227,12138.492987,353424.717227,198886.396356]` | `8962F01D707AA1EF8E27A6D7BE06D53FAF49C6FE3550054AF9713FA3157973F9` | 与阶段一一致 |

## 7. Web 性能调查

使用容差 `0.01`、Unlimited Profile，每份文件连续运行 5 次；稳定中位数取后 4 次中间两项平均值。

| 文件 | 阶段三稳定总中位数 | P4-8 稳定总中位数 | P4-8 范围 | 结论 |
|---|---:|---:|---:|---|
| `block_test_minimal.dxf` | 0.274 ms | 0.082 ms | 0.058～0.282 ms | 无回退 |
| `slock_spline_zhenlie.dxf` | 50.046 ms | 23.398 ms | 21.086～31.097 ms | 无回退 |
| `ship2_block_test.dxf` | 170.182 ms | 92.581 ms | 72.701～97.022 ms | 无回退 |

该数据只用于识别明显回退，不作为阶段四性能提升承诺。耗时会受 Node JIT 和系统负载影响。

## 8. 工作区边界

P4-8 计划提交的文件只有：

```text
package.json
scripts/verify-geometry-baseline.js
docs/阶段四_小步重构实施方案.md
docs/阶段四_P4-8_全量回归与阶段收口报告.md
```

以下工作区已有内容不属于 P4-8，不会随本批次提交：

```text
docs/G6_Agent密钥与上游请求安全修复说明.md
docs/O级别问题分组修复方案.md
page/README.md
server/geometry-tools.js
server/http-utils.js
test/test_parser.cpp
tests/agent-boundaries.test.js
tests/agent-tools.test.js
tests/server-routes.test.js
tests/web-editor-modules.test.js 中未提交的失败边界用例
阶段三未提交文档、临时目录和构建目录
```

## 9. 最终 SAM GUI 验收

### 9.1 部署

部署并重启 SAM：

```text
bin/Release/Example1.pyd
bin/Release/SAM.Pre.Example1Toolset.dll
```

### 9.2 最小图精确验收

输入：

```text
example\block_test_minimal.dxf
Profile: Small drawing
Base X/Y/Z: 0 / 0 / 0
Tolerance: 0.01
```

Sketch 预期：

```text
正好 3 条直线
(1000,1000,0) -> (1100,1000,0)
(400,300,0)   -> (410,300,0)
(400,300,0)   -> (400,310,0)
```

FE 使用 `Model-1 / P4_final_small_FE`，预期：

```text
5 个节点
3 个 Truss
包围盒 Min=(400,300), Max=(1100,1000)
```

### 9.3 阵列与曲线图

```text
example\slock_spline_zhenlie.dxf
Profile: Large drawing
Mode: Sketch，然后 Finite Element
FE Part: P4_final_slock_FE
```

确认：块、阵列、镜像和曲线无缺失或整体错位；Sketch 与 FE 轮廓一致；日志提交数量与 Repository 一致。

### 9.4 船体图、取消与重试

```text
example\ship2_block_test.dxf
Profile: Unlimited
```

至少执行 Sketch；资源允许时执行 FE。另使用 `P4_final_cancel_FE`：

1. 创建前点击 Cancel；
2. 创建节点或单元过程中点击 Cancel；
3. 检查不存在半成品 Part、节点或单元；
4. 使用同名 Part 重试并成功；
5. 记录取消响应、总耗时和峰值内存。

### 9.5 状态、路径和连续性

- [x] Small/Large/Unlimited 日志上限分别为 `100000/500000/-1`；
- [x] Ignore Layers 只移除选中图层；
- [x] 中文路径 Sketch 与 FE 均成功；
- [x] 日文路径 Sketch 与 FE 均成功；
- [x] 连续导入 5 次无进度窗口、回调或上次状态残留；
- [x] 日志查看器停止、切换、关闭和重开正常；
- [x] 既有 SAM 对象在所有用例后仍然存在。

更详细记录表可直接使用：

[阶段三 SAM GUI 人工验收操作手册](./阶段三_SAM_GUI人工验收操作手册.md)

## 10. 阶段五移交候选

以下项目不属于阶段四正确性重构，移交性能阶段：

1. 在真实 SAM GUI 中建立 Sketch/FE 解析、转换、创建、总耗时和峰值内存的稳定基线。
2. 基于 Profile 和硬件数据评估 Sketch 创建批大小与取消检查频率。
3. 基于真实大图评估 FE Python 命令批大小，避免只凭字符串长度推测优化。
4. 分析 libdxfrw 解析、曲线离散、节点合并和 Python/SAM 提交的耗时占比。
5. 对超过 10% 的可重复回退建立专项基准和性能门禁。
6. 第三方 Qtitan/Python 2.7 编译警告作为 SDK 升级或警告隔离任务处理，不混入性能改写。

## 11. 验收判定

满足以下条件即可回复“通过”：

- [x] 最终干净构建成功；
- [x] CTest 14/14 通过；
- [x] Node 测试和几何基线门禁通过；
- [x] 最小图 Sketch 为 3 条精确直线，FE 为 5 节点/3 Truss；
- [x] 阵列曲线图 Sketch/FE 轮廓一致；
- [x] 船体图可完成或取消后完全回滚；
- [x] 同名重试、Unicode 路径、Ignore Layers 和连续导入正常；
- [x] 未发现对象残留、崩溃、明显性能回退或既有对象被修改。

用户已于 2026-08-03 确认最终验收通过；P4-8 已完成复核、提交和推送，阶段四正式完成。
